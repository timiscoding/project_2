// var app = app || {};

// app.FeedbackListPageView = Backbone.View.extend({
//   el: '#main',

//   render: function () {
//     // Gets the FeedbackPageViewTemplate from our app.html.erb to setup the view structure
//     var feedbackListPageViewTemplate = _.template($('#feedbackListViewTemplate').html());
//     // Sets #main HTML to feedback view template's HTML
//     this.$el.html(feedbackListPageViewTemplate);
//   }

// });